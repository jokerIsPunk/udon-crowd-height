Limit Zones and Non-Limit Zones can overlap and be combined with boolean operators. See the Complex Zones example scene.

Note that a player is defined as inside a zone only if their head is inside the box collider.